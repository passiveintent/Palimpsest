# 30-day byte ledger (M2 benchmark)

Generated by `plsim --month` (seed 43): 6 simulated days, 10000 raw per-instance series (2000 logical groups × 5 pods → 6000 sketched series per ADR-008), 8 emitters, flush 10s, keyframe every 6 windows (golden every 10 keyframes), codec per --codec, storm 25× over 30 windows, FALLBACK top-K 100.

## Totals

| line | bytes | GiB | vs Palimpsest |
| --- | --- | --- | --- |
| baseline remote-write, uncompressed | 83382184800 | 77.66 | 64.1× |
| baseline remote-write, snappy (RW 1.0) | 15656296680 | 14.58 | **12.0×** |
| baseline remote-write, zstd | 8454265200 | 7.87 | 6.5× |
| **Palimpsest, everything** | 1301709696 | 1.212 | 1× |

| Palimpsest component | bytes | share |
| --- | --- | --- |
| golden keyframes | 505086325 | 38.8% |
| KDELTA keyframes | 77685445 | 6.0% |
| RESIDUAL sketches | 718849969 | 55.2% |
| FALLBACK frames | 87957 | 0.0% |
| dashcam snapshot blobs (subset) | 55282 | 0.0% |

## Incidents

| incident | injected | substrate (a) support / emit | substrate (c) drift | storm behavior | notes |
| --- | --- | --- | --- | --- | --- |
| spike | window 12963, 3m0s | 0s / never (support 47%, gated 100%) | 30s (max Δ 51.50 vs threshold 10) | never fired | sustained shift: substrate (a) sees it only until the next keyframe re-bases (ADR-003); substrate (c) owns it from there |
| leak | window 24771, 1h12m0s | 8m20s / never (support 11%, gated 100%) | 1h12m30s (max Δ 19.26 vs threshold 10) | never fired | climb is dead-zone by design; a substrate hit near span end is the collapse when the ramp stops (the OOM kill), not the cause; ring-buffer lookback covers the final 20.8% of the climb |
| azdown | window 37299, 6m0s | never / never (support 0%, gated 100%) | 30s (max Δ 101.87 vs threshold 10) | 24 fallback windows, active 0s→20s of the span; top-K coverage 99% (1821/1848) | the breaker fires only until the next keyframe re-bases the outage into every baseline (ADR-003), then RESIDUAL frames resume against a fleet-wide re-based state; the recovery transient at outage end fires it again |

## Quiet-window recovery health

129 solves outside incident spans: 100% gated at the palimpsestd default max-residual 5; mean solve residual 19.91; mean support size 196.

## Honest accounting notes

- The baseline is per-instance remote-write (labels repeated every window, as RW 1.0 actually behaves), snappy-compressed with a real compressor; the zstd row is a generous baseline no stock Prometheus ships today.
- Palimpsest's ledger includes every escape hatch that fired: FALLBACK frames, dashcam snapshot blobs, dict deltas, golden re-announcements under churn.
- Dashcam snapshots here are per logical series (plsim's pipeline); the otel processor buffers per instance, so real snapshot bytes scale with flagged-series instance counts — see docs/ENVELOPE.md §6-8.
- Pod churn never touches the sketch (ADR-008): only the baseline pays label-set turnover. Logical churn is charged to Palimpsest via dict deltas and golden keyframes (ADR-017).
- Detection columns are measured against palimpsestd defaults; see docs/DEADZONE.md for why the leak is invisible and what it would take to see it.
